from setuptools import setup

setup(
   name='autodiff',
   version='0.1',
   description='simple automatic differentiation for statistical analysis',
   author='ttpr0',
   packages=['autodiff'], 
   install_requires=['numpy'], 
)